# -*- coding: utf-8 -*-
"""
ℹ️ このアプリについて - 技術情報とクレジット
"""

import streamlit as st

st.set_page_config(page_title="ℹ️ このアプリについて", layout="wide")

st.title("ℹ️ AI画像認識 体験デモについて")

if st.button("← ライブデモに戻る", use_container_width=True):
    st.switch_page("pages/01_live_demo.py")

st.divider()

# ============================================================
# プロジェクト概要
# ============================================================

st.header("📚 プロジェクト概要")

st.markdown("""
このアプリケーションは、現代の AI 技術がどのようにコンピュータビジョン（画像認識）の
問題を解決しているのかを、**実際に体験**できるデモです。

オープンキャンパスで来場された学生さんに、
「AI ってこんなことができるんだ！」という驚きや興味を
感じてもらうことを目的に開発されました。

### 🎯 実装の特徴

- **リアルタイム処理**: 複数の AI モデルが同時に動作
- **複数人対応**: 顔2個、手2個を同時認識
- **軽量化**: GPU なしでも 30FPS 以上を実現
- **インタラクティブ**: ジェスチャーに即座に反応
""")

st.divider()

# ============================================================
# 使用技術
# ============================================================

st.header("🛠️ 使用技術")

col1, col2 = st.columns(2)

with col1:
    st.subheader("AI・機械学習")
    st.markdown("""
    - **MediaPipe** (Google)
      - Face Mesh: 顔の詳細検出
      - Hand Tracking: 手の認識
      - Pose Estimation: 全身ポーズ判定

    - **OpenCV**
      - 画像処理と視覚化

    - **JAX / PyTorch**
      - バックエンドの高速演算
    """)

with col2:
    st.subheader("Web・フロントエンド")
    st.markdown("""
    - **Streamlit**
      - Web UI フレームワーク
      - リアルタイム更新
      - マルチページ対応

    - **Python**
      - バックエンド実装言語
      - 数値計算（NumPy）
    """)

st.divider()

# ============================================================
# アーキテクチャ
# ============================================================

st.header("🏗️ システムアーキテクチャ")

st.markdown("""
```
┌─────────────────────────────────────┐
│      Streamlit Web UI               │
│  (Pages / Navigation / Controls)    │
└──────────────────┬──────────────────┘
                   │
┌──────────────────▼──────────────────┐
│      メイン処理ループ                 │
│  (30FPS リアルタイム)                │
└──────────────────┬──────────────────┘
                   │
        ┌──────────┼──────────┐
        ▼          ▼          ▼
    ┌────────┐ ┌────────┐ ┌────────┐
    │ Face   │ │ Hand   │ │ Pose   │
    │ Mesh   │ │Track   │ │Est.    │
    │(Google)│ │(Google)│ │(Google)│
    └────┬───┘ └───┬────┘ └───┬────┘
         │         │          │
    ┌────▼─────────▼──────────▼────┐
    │   エフェクト・レンダリング      │
    │  (OpenCV / PIL)              │
    └────┬─────────────────────────┘
         │
    ┌────▼──────────────────────────┐
    │    ビデオ表示 (JPEG圧縮)      │
    │  (Streamlit video_placeholder)│
    └───────────────────────────────┘
```
""")

st.divider()

# ============================================================
# 技術的な工夫
# ============================================================

st.header("💡 技術的な工夫")

with st.expander("📊 パフォーマンス最適化"):
    st.markdown("""
    ### 1. 画像の前処理
    - キャプチャ解像度: 960×540
    - 推論用サイズ: 480×270（50%スケール）
    - 推論時間を約75%削減

    ### 2. キャッシング
    - フォント・画像の一度限りロード (`@st.cache_resource`)
    - 絵文字キャッシュで重複生成を防止
    - 猫耳メモリの再利用

    ### 3. バッチ処理
    - テキスト描画をまとめて1回で実行
    - PIL → OpenCV 変換の最小化
    - JPEG 圧縮で転送量を削減

    ### 4. フレームレート制御
    - リングバッファ（buffersize=1）で遅延削減
    - FPS 計算と表示
    """)

with st.expander("🧠 AI 推論の工夫"):
    st.markdown("""
    ### 1. 複数モデルの同時実行
    - Face Mesh + Hand Tracking + Pose を並列処理
    - 依存関係がないので独立実行

    ### 2. ジェスチャー判定の精度向上
    - 直近5フレームの多数決で誤検出を削減
    - 最低3回の一致で確定 (`GESTURE_MIN_VOTES`)
    - 指の伸び具合を距離ベースで判定

    ### 3. 手のサイズ正規化
    - 手の大きさを基準に相対値で判定
    - カメラからの距離に関係なく認識
    - 異なる体格でも対応
    """)

with st.expander("🎨 ビジュアル効果"):
    st.markdown("""
    ### 1. 複数層の描画
    - レイヤー1: OpenCV（リング、星、装飾）
    - レイヤー2: PIL テキスト（日本語、ストロークなし）
    - レイヤー3: 絵文字（RGBA 透明処理）
    - 最後に JPEG 圧縮で効率化

    ### 2. アニメーション
    - リングの拡散: フェーズベースの連続アニメーション
    - きらめき星: 三角関数で回転
    - 紙吹雪: パターン化で計算量最小化

    ### 3. 顔の装飾
    - ベジェ曲線で滑らかな猫耳を生成
    - 回転行列で視点に合わせた装着
    - アルファブレンディングで自然な合成
    """)

st.divider()

# ============================================================
# 今後の改善案
# ============================================================

st.header("🚀 今後の改善案（ロードマップ）")

with st.expander("短期（1-2ヶ月）"):
    st.markdown("""
    - [ ] スコアランキング機能（デモ中に得点表示）
    - [ ] 異なるサングラス・装飾の追加
    - [ ] リーダーボード（ローカルストレージ）
    - [ ] 日本語以外の言語対応
    """)

with st.expander("中期（3-6ヶ月）"):
    st.markdown("""
    - [ ] フルボディダンス認識（12-15ポーズ）
    - [ ] 物体認識（オブジェクトの名前表示）
    - [ ] リアルタイム3Dモデル合成
    - [ ] 音声ガイドの多言語対応
    - [ ] モバイル版（スマートフォン対応）
    """)

with st.expander("長期（6-12ヶ月）"):
    st.markdown("""
    - [ ] ローカルネットワークでの複数デバイス連携
    - [ ] AR フィルター拡張（背景変更など）
    - [ ] 動画の録画・再生機能
    - [ ] より高度な AI モデル（顔の感情認識など）
    - [ ] 自作 AI モデルのトレーニング機能
    """)

st.divider()

# ============================================================
# クレジット・参考文献
# ============================================================

st.header("📝 クレジット")

st.subheader("オープンソース・ライブラリ")

libs = {
    "MediaPipe": "https://mediapipe.dev/",
    "OpenCV": "https://opencv.org/",
    "Streamlit": "https://streamlit.io/",
    "NumPy": "https://numpy.org/",
    "Pillow (PIL)": "https://python-pillow.org/",
    "PyTorch": "https://pytorch.org/",
    "JAX": "https://github.com/google/jax",
}

cols = st.columns(2)
for i, (lib, url) in enumerate(libs.items()):
    with cols[i % 2]:
        st.markdown(f"- [{lib}]({url})")

st.subheader("参考資料")

st.markdown("""
- [MediaPipe Documentation](https://mediapipe.dev/)
- [OpenCV Tutorials](https://docs.opencv.org/)
- [Streamlit Documentation](https://docs.streamlit.io/)
- [Computer Vision Basics](https://en.wikipedia.org/wiki/Computer_vision)
""")

st.subheader("開発者")

st.info("""
👨‍💻 **開発**: AI教育デモチーム

📧 お問い合わせやご質問は、スタッフまでお気軽にどうぞ。

このアプリケーションは教育目的で開発されました。
技術的なご質問や改善提案も大歓迎です！
""")

st.divider()

# ============================================================
# よくある質問
# ============================================================

st.header("❓ よくある質問")

with st.expander("Q: なぜ手が認識されないことがあるのですか？"):
    st.markdown("""
A: MediaPipe は手の骨格を学習するために大規模なデータセットで
訓練されていますが、以下の場合は認識が難しくなります：

- 手が小さく見える（カメラから遠い）
- 照明が不十分
- 手が隠れている、または重なっている
- 複雑なジェスチャー（指が重なっているなど）

**対策**: カメラに近づく、照明を改善してみてください。
    """)

with st.expander("Q: GPUがなくても動きますか？"):
    st.markdown("""
A: はい、動きます！このアプリケーションは CPU でも十分に
30FPS 以上で動作するように最適化されています。

- **推奨**: Core i5 以上の CPU
- **メモリ**: 4GB 以上
- **GPU**: あるとさらに高速（NVIDIA CUDA 対応）
    """)

with st.expander("Q: プライバシーはどうなっていますか？"):
    st.markdown("""
A: **完全にローカルで処理しています**。

- ✅ 画像や動画はクラウドに送信されません
- ✅ キャプチャデータは保存されません
- ✅ カメラ入力はリアルタイム処理のみ使用

すべての処理はあなたのマシン上で行われます。
    """)

with st.expander("Q: このアプリケーションのソースコードは？"):
    st.markdown("""
A: GitHub で公開されています！

https://github.com/your-repo/videoanalyze

- 自分用にカスタマイズ
- 新しい機能の追加
- バグ報告や改善提案

など、ご自由にどうぞ！
    """)

st.divider()

col1, col2, col3 = st.columns(3)
with col1:
    if st.button("🎬 ライブデモ", use_container_width=True):
        st.switch_page("pages/01_live_demo.py")
with col2:
    if st.button("🏠 ホーム", use_container_width=True):
        st.switch_page("app.py")
with col3:
    if st.button("✌️ ジェスチャーガイド", use_container_width=True):
        st.switch_page("pages/02_gesture_guide.py")
